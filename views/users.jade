extends layout
block title
    title= "Users"
block content
    table
        - for (var i = 0; i < usernames.length; i++) {
            tr
            td
                a(href='/user/' + keys[i])= usernames[i]
        - }